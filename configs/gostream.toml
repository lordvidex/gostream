[client]
servers = ["localhost:50051", "localhost:50052", "localhost:50053"]
connections = 10
[server]
redis_url="redis://localhost:6379"
dsn="postgres://postgres:postgres@localhost:5432/gostream" # TODO: change values, this is based on values from env.example
