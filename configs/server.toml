[server]
dry_run = false
grpc_port = 50052
http_port = 8081
log_file = ''
# dsn = ... # better set from .env file (see env.example)
# redis_url = ... # better set from .env file (see env.example)
run_migrations = true
